let defaultState = { 
    loadingInfo: {
        isLoading: false,
        toLoadCount: 0,
        loadedCount: 0
    }
}

const toolbar = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default toolbar